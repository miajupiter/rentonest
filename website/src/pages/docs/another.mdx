# Another Page

```js showLineNumbers filename="demo.js" {3} copy
let a = 1;

console.log(a);
```

```html showLineNumbers filename="deneme.html" /h1/
<div>
  <h1>My Theme</h1>
  <div style={{ border: '1px solid' }}>
    {children}
  </div>
</div>
```

## Component

import { useState } from 'react'

{/* Import CSS modules */}
import styles from '@/ui/components/counters.module.css'

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>Clicked {count} times</button>
    </div>
  );
};

<Counter/>

## External Component

import Counters from '@/ui/components/counters'

<Counters />
